import React from "react";
import s from "./Footer.module.css"

export const Footer = () => {
    return (
        <div className={s.footer}>
            <div className={s.item}>Footer</div>
        </div>
    );
};
